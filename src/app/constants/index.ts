const APP_CONSTANTS = {
    apiPrefix: "/v1",
    params: "params",
    query: "query",
    body: "body",

    // Add the short name of the service below
    service: "svc"
};

export default APP_CONSTANTS;
